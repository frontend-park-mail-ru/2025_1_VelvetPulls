<nav class="popover">
    {{#each items}}
        <button class="popover__item">
            <img src="{{svg}}" alt="?" class="popover__item__icon"></img>
            <span name="{{name}}" class="popover__item__text">{{content}}</span>
        </button>
    {{/each}}
</nav>