export class Contact {
    constructor({ fullName, avatarPath, onlineStatus }) {
        this.fullName = fullName;
        this.avatarPath = avatarPath;
        this.onlineStatus = onlineStatus;
    }
}
