export class User {
    constructor({ username, firstName, lastName, avatarUrl }) {
        this.username = username;
        this.firstName = firstName;
        this.lastName = lastName;
        this.avatarUrl = avatarUrl;
    }
}
