<nav class="popover">
    {{#each items}}
        <button class="popover__item" id="{{this.id}}">
            <img src="{{this.svgPath}}" alt="?" class="popover__item__icon"></img>
            <span class="popover__item__text">{{this.content}}</span>
        </button>
    {{/each}}
</nav>
