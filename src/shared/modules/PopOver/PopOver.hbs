<div class="popover">
    {{#each items}}
        <button class="popover__item" id="{{this.id}}">
            <img src="{{this.svgPath}}" alt="?" class="icon"></img>
            <span class="popover__item_text">{{this.content}}</span>
        </button>
    {{/each}}
</div>
